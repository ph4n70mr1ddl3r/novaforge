# DR restore-drill execution record — 2026-08-28 (PHASE-8 §8, T10)

Scope executed against the live compose stack (the runbook's drill, one pass):
base snapshot → restore to a DR instance → the §6 exit criteria. The
PITR/MinIO-versioning/Kafka-replay legs are configured-state findings below —
the drill found them unconfigured, which is the honest acceptance outcome.

## The restore (executed)

1. **Snapshot inventory**: ad-hoc base snapshot — `pg_dumpall` (all 13 databases:
   metadata, data, audit, workflow, notification, scheduler, integration, file,
   Keycloak's realm store, platform) → 196 MB gzipped, **26 s** capture.
   *Finding: the nightly backup jobs the runbook assumes are not configured on
   the compose stack — no cron, no pg_dump job, no WAL archiving. The drill's
   snapshot was operator-initiated.*
2. **DR target**: a second Postgres 16.15 (`novaforge-dr-postgres`, :5435), fresh
   volume — the runbook's DR instance stand-in.
3. **Restore**: full dump replay — **3 m 07 s**, all 13 databases back, zero errors
   beyond pre-existing roles/databases.
4. **Exit criteria (the runbook's §6 list, verified on the restored instance)**:
   - published apps: **all 5** present with versions (Aaa-journey v1, ArDesk v2,
     Erp v5, PerfHook v1, Purchasing v1);
   - record data: **1,000,231 PerfDoc rows** + the full ERP corpus
     (JournalEntry 177, Account 168, …) — count-exact vs the source;
   - audit trail: **1,934 events, max(occurred_at) identical to the source** —
     the pre-restore trail is fully served from the restore.
5. **RPO achieved**: snapshot-granular (the dump's capture instant, ~1 min of
   wall between capture and consistency) vs the ≤5 min WAL target — **met for
   this drill**, but only because the drill's own snapshot was the RPO boundary.

## Configured-state findings (closed same-day 2026-08-28 — the backup configuration now lands, and the PITR leg runs for real)

| # | Finding | Severity | Disposition |
|---|---------|----------|-------------|
| D-1 | **WAL archiving off** (`archive_mode=off`) — PITR impossible as configured | high | **CLOSED**: `postgres` runs `archive_mode=on` archiving to the `wal-archive` volume (via `deploy/compose/postgres/entrypoint.sh`, which chowns the volume before the image's gosu drop — a fresh root-owned volume is otherwise unwritable by the server user); `deploy/postgres-init/02-replication-hba.sh` admits the sidecar's replication connection. The restore is `deploy/scripts/pitr-restore.sh`, **executed live**: base + archived WAL replayed to a 14:58:30Z target and promoted — count-exact vs the primary (967 metadata definitions, 1,000,947 records, the audit trail ending exactly at the pre-target boundary, 31 realm users) |
| D-2 | **Nightly jobs absent** — no pg_dump cron, no realm-export schedule | high | **CLOSED**: the `postgres-backup` sidecar (`deploy/compose/backup/nightly-backup.sh`) — nightly `pg_dumpall` (188 MB gz on this stack) rotated to the newest 7, plus a weekly physical `pg_basebackup` for the PITR leg; **verified live** (dump + base both taken, rotation trimming). The Keycloak realm rides the dump (the `keycloak` database is in the cluster — the drill restored it); the standalone partialExport applies to staged deployments running Keycloak on its own store |
| D-3 | **MinIO bucket un-versioned** — object overwrite/delete unrecoverable | medium | **CLOSED**: the `minio-init` one-shot enables bucket versioning at stack bring-up — **verified live** (`mc version info`: versioning enabled on `novaforge-attachments`) |
| D-4 | Kafka topics carry default retention (implicit) | low | **CLOSED**: `KAFKA_LOG_RETENTION_HOURS=168` pinned in the compose broker — **verified live** (`log.retention.hours=168` in the running server.properties) |
| D-5 | The compose `dr` profile does not exist | medium | **CLOSED**: the `dr` profile carries `dr-postgres` (:5435, WAL archive mounted read-only) + `dr-minio` (:9100/9101) — **verified live** (the D-1 PITR restore ran into `dr-postgres`; note podman-compose 1.0.x ignores profiles and starts them unconditionally — harmless DR standbys, one `down` away) |

The secrets-rotation leg (§8) is covered by `IntegrationWebhookTests`' rotation
cases (two-active-window verify → retire → exactly-one-active), re-run in CI;
**the live rotation also executed 2026-08-28** on the demo `bankFeedKey`
credential through the builder API: provision → dual-active window (two active
versions observed in `it_secrets`) → retire → exactly one active remaining.

## Verdict

Restore-path acceptance: **PASS** (full-fidelity restore, all exit criteria,
RPO bounded by snapshot cadence) — **and the configured-state legs now pass
too**: WAL archiving live, nightly snapshots + weekly base running with
rotation, bucket versioning on, retention pinned, the `dr` profile one command
away, and the PITR restore (base + WAL to a chosen timestamp) proven count-exact
on this stack. The drill did its job by finding the gaps before a real DR event
would have; the same-day closures turned both acceptance rows green.
