# Pen-pass execution record — 2026-08-28 (PHASE-8 §8, T9)

Method per `security-review-scope.md`: the suite baseline (the suites encode the
intended verdicts — CI's `./mvnw verify` runs them on every PR) plus live
adversarial probes against the deployed stack. Driver:
`docs/loadtests/pen-pass-probes.py` (scratch tenants, synthetic actors — one
provisioned tenant + admin per run; no production data touched).

## Live probes — 15/15 PASS

| Probe | Verdict observed |
|---|---|
| Unauthenticated API access | 401 problem+json (never a stack trace) |
| Cross-tenant list (fresh tenant admin → demo-tenant entity) | 400/4001 — isolated, no rows |
| Cross-tenant record id addressing | 400/4001 — no existence leak |
| Cross-tenant record write (PATCH) | denied |
| Cross-tenant record delete | denied |
| Tenant admin ≠ platform admin (admin surfaces) | 403 |
| Audit trail read as non-platform-admin | 403 |
| Inbound webhook, stale timestamp | 401/4012 SIGNATURE_INVALID |
| Inbound webhook, mangled signature | 401/4012 — indistinguishable from stale |
| Inbound webhook, unknown hook id | 404 (see findings) |
| Tampered promotion artifact (bit-flipped signed zip) | 400/4000 rejected |
| Pristine artifact import (the control leg) | 200 — the rejection is the tamper, not the pipe |
| Webhook prefix under sustained anonymous load | limiter engages (93/150 → 429); HMAC still gates every accepted call |
| Internal hook surfaces (`/api/v1/hooks/**`) via gateway | unrouted (404) |
| Tenant provisioning as the fixture path | 200 — the admin user lands in the realm with the pinned password |

The HMAC legs ran against the ERP app's published `paymentsFeed` hook (published
into the demo tenant for the pass — the run's only workspace mutation besides the
scratch tenants and the two import apps, both named for cleanup).

## Findings (triaged, the §8 discipline)

- **PF-1 (low, backlog): hook existence enumerable on the anonymous webhook route.**
  A nonexistent hook answers 404/4004 while a known hook with a bad signature
  answers 401/4012 — an anonymous caller can distinguish "hook exists" from
  "signature wrong". The §6 indistinguishability pin covers wrong-secret/stale/
  replayed on known hooks (all 4012 — verified); unifying the unknown-hook answer
  to 401 is a small follow-up, recorded here rather than fixed in-pass.
- **PF-2 (low, backlog): wrong-shape webhook URLs answer 500 INTERNAL.**
  `POST /api/v1/webhooks/inbound/{single-segment}` (not the
  `/{tenant}/{entity}/{hookId}` shape) surfaces the container's NoResourceFound as
  500 instead of 404 — noise for monitoring, no information leak. Map
  NoResourceFoundException to 404 in the integration service's ProblemAdvice.

## Suite baseline (the owning suites, re-run in CI on this tree)

Authz matrix/field security/sharing (`RecordApiTests`, `SharingTests`,
`ReportAggregateTests`), RLS fixtures (`test-support`), sandbox escape
(`ScriptSandboxTests`, `ScriptApiTests`), HMAC matrix + rotation
(`IntegrationWebhookTests`), rate limiter (`WebhookRateLimitFilterTest`),
promotion override/artifact tamper (`LifecycleTests`) — all green in the tree's
`./mvnw verify` (the CI build job); the 2026-08-28 local run of the touched
modules (engine, gateway) is green with the pass's fixes.

## Manual legs

- Dependency CVE scan: deferred to the CI dependency-review job (the tree pins
  Boot 4.1.0/Framework 7.0.8 — the Aug-2026 line; no known criticals against the
  pinned set at pass date).
- JWT claim trust boundary: `tenant_id`/`platform_roles` ride the realm's claim
  mappers (the re-imported realm verified); services derive tenant from the token,
  never the gateway header (Phase 0's pin, re-verified live by the cross-tenant
  probes above).
- Gateway route table vs service network: the only anonymous route is the webhook
  prefix (rate-limited); internal surfaces (`/api/v1/hooks/**`) are unrouted —
  probed live.
