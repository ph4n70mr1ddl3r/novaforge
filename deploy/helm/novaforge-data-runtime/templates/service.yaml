apiVersion: v1
kind: Service
metadata:
  name: novaforge-data-runtime   # flat: env wiring references these DNS names verbatim (values are data — no release templating)
  labels:
  {{- include "novaforge-data-runtime.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector:
  {{- include "novaforge-data-runtime.selectorLabels" . | nindent 4 }}
