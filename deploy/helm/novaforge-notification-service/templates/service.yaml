apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-novaforge-notification-service
  labels:
  {{- include "novaforge-notification-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector:
  {{- include "novaforge-notification-service.selectorLabels" . | nindent 4 }}
