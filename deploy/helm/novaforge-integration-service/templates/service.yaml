apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-novaforge-integration-service
  labels: {{ include "novaforge-integration-service.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector: {{ include "novaforge-integration-service.selectorLabels" . | indent 4 }}
