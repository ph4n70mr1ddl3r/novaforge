apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-novaforge-file-service
  labels:
  {{- include "novaforge-file-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector:
  {{- include "novaforge-file-service.selectorLabels" . | nindent 4 }}
