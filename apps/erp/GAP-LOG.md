# Phase 7 Gap Log — the ERP Dogfood

> A phase deliverable (PHASE-7 §1 rule 2): **every gap becomes a log entry before any
> workaround**. Triage at the weekly review assigns `accept-as-platform-feature |
> backlog | wontfix-with-workaround`; accepted items become versioned platform features
> with a spec section here before implementation (§8). The two §3 candidates were
> pre-accepted, confirmed by the dogfood, and shipped as platform features this phase.
>
> This markdown is the human-readable log; the same entries ride `erp-app.json`'s
> `gapLog` branch (versioned metadata — PHASE-8 §3's change-set review renders the
> entries a promoting version resolves). Keep the two in sync when triage moves.

| # | Area | Blocker | Workaround in the ERP v1 | Proposed primitive / flag | Priority | Disposition |
|---|------|---------|--------------------------|---------------------------|----------|-------------|
| G-1 | P3 Business Logic | **Auto-journal from an invoice:** `createRecord` cannot bind lines to the entry it just created — the created record's id is not captured into flow scope, and `${…}` template resolution is top-level only, so an inline `lines` array inside a `createRecord` template passes through unresolved (§5's "createRecord journal lines from templates" is not expressible) | Historical v1: the ar clerk booked the invoice's journal as its own `JournalEntry` (the workaround this entry logged before the harvest); **adopted 2026-09-02** — the invoice posting flow now creates the journal itself (`createRecord` with deep-resolved lines — posting accounts and `totalBook` on the invoice, `sourceInvoice` linking back), and the reconciliation/credit-currency suites assert the auto-created entry | `createRecord` captures the created id into step scope (`${step.id}`) + nested template resolution for children arrays | **high** | closed — shipped as the versioned platform feature (spec §3.3, 2026-08-26): `${record.<stepId>.<path…>}` step results + deep template resolution; adopted by the corpus 2026-09-02 |
| G-2 | P3 Business Logic | **Cross-record arithmetic in flows:** `updateRecord` templates interpolate (`${field}`) but cannot compute over multiple records — the receipt-side weighted-average re-costing of `Item.unitCost` (`(qtyOld×costOld + qtyNew×costNew) / (qtyOld + qtyNew)`) is inexpressible declaratively | Historical v1: roll-up maintained `inventoryValue`/`qtyOnHand` kept the running average *derivable*, and the one budgeted script costed issues from those roll-ups (§5's canonical escape-hatch case) — with the ≤ 20% ceiling exceeded at exit (1 script of 4 hooks = 25%, the Inventory module 1/1) under this entry's reviewed exception. **Adopted 2026-09-03**: the costing hook is the §3.7 declarative flow, the script is gone (0 of 4 hooks — the ceiling holds), and the `inventoryCosting` suite pins the identical numbers | expression-valued template slots (or a `computeField` primitive evaluated over queried bindings) → shipped as the `bind` primitive (a flow step binds a lookup target's canonical view into expression scope) | **high** | closed — shipped as the versioned platform feature (spec §3.7, 2026-09-03): `item.<field>` dot-paths in flow expressions, statically typed at compile against the bound target; the weighted-average costing is declarative (script ratio 0/4) |
| G-3 | P1 Data Modeling | **Rollup fields on `amountOutstanding`:** posting should stamp `amountOutstanding = total` once, but defaults evaluate before children persist and the resume write only carries the state field | The suite/clerk sets `amountOutstanding` at invoice create; settlement decrements it on the POSTED invoice (per §2's pin — the invoice is not frozen, only its journal entry is; the bankFeed suite pins the decrement) | rollup-derived initial values, or a `stampOnTransition` hook context | medium | backlog |
| G-4 | Script sandbox | **Decimal fidelity in scripts:** GraalVM JS computes in float64; money through `$data.query` reads and script arithmetic is not BigDecimal-exact by construction (PLAN.md §1 non-negotiable) | The costing script's arithmetic stays exactly representable in the dogfood corpus (÷ by whole quantities); assertions pin the decimal-exact roll-ups the *engine* computes, not script floats | a `$decimal` BigDecimal binding in the sandbox surface | **high** | closed — shipped as the versioned platform feature (spec §3.4, 2026-08-26): `$decimal`, exact by construction (string/integral construction, DSL-method vocabulary, exact string crossing) |
| G-5 | P1/P3 | **`$data.query` cannot filter by `id`** (system fields are not authored fields, so the query-DSL leaf rejects them) — the costing script lists Items and matches `id` in JS | Script scans the first page (≤200 rows) and matches client-side | allow `id` (and `version`) as filterable system fields in the DSL leaf | medium | closed — shipped as the versioned platform feature (spec §3.6, 2026-08-27): id/version join the DSL leaf set, canonical values parsed at the door, other reserved names stay rejected |
| G-6 | P3 hooks | **Once-only hook triggers:** the `afterSave` posting flow re-suspends on every save of a `SUBMITTED` record (a memo edit re-requests approval); with the auto-journal (2026-09-02) each resolution of a re-suspended posting approval also creates another draft `JournalEntry` | Guards read the state field, and the journeys save `SUBMITTED` exactly once (exactly one journal per invoice); rejection leaves the record `SUBMITTED` (a resubmit re-suspends — acceptable, but noisy) | idempotency flag on `requestApproval` (suspend once per record+step) | medium | backlog |
| G-7 | P2 UI Builder | **No builder/runtime UI** (the Phase 2 remainder): the ERP is authored as JSON through the definition APIs; runtime users have no pages yet | `apps/erp/*.json` + suites are the authored artifact; `ErpAppArtifactTests` gates them through the exact save/compile checks the builder would run | (the Phase 2 builder shell — already the tracked remainder) | **high** | closed — the Phase 2 shells landed after the dogfood (builder + runtime), and the 2026-08-24 review closed Phase 4 §11's remaining surfaces |
| G-8 | P5 Reporting | **Weighted-average unit cost as a report column:** `avg(unitCost)` is not the weighted average; the true average is `inventoryValue / qtyOnHand`, an aggregate-of-aggregates the v1 report vocabulary cannot express | `inventoryValuation` reports the value/qty totals; the average is derivable (and asserted in the suite by dividing the literals) | computed (ratio) aggregate columns in `ReportDefinition` | low | wontfix-with-workaround |
| G-9 | Multi-currency | **Realized gain/loss on settlement** requires an allocation flow over payments × invoices with FX revaluation at payment-date rates — expressible with today's primitives only as per-case scripts, blowing the script budget | v1 scope pins book currency USD, document-currency conversion at the document rate (`fxRate` carried, GL posts in book currency); settlement gain/loss is logged, not automated (§2's pin: revaluation/unrealized is a logged gap, and realized joins it) | an `allocate` primitive (payment → invoices/credit memos, FIFO or specific) | high | backlog |
| — | FIFO costing | Deferred by §11 Q1 (resolved): weighted-average only; lot machinery half-present in `StockLedger` | — | `costingMethod` flag on Item + lot allocation | low | backlog (versioned feature) |
| — | Full dunning automation | Deferred by §11 Q2 (resolved): letters + scheduler only | `nightlyAging` scheduled delivery is the v1 dunning surface | escalation chains (Phase 4 reuse) | low | backlog |
| G-10 | P4 Workflow | **Reopen resolves only through its own approval flow (§4):** transition guards and hook guards evaluate the record's *current* (post-merge) state — a `status == 'OPEN'` guard cannot distinguish the audited `CLOSED → OPEN` reopen from the initial create, so gating reopen with `requestApproval` would suspend every period creation (and SoD would block the creating controller from approving it) | The reopen edge is a listed, audited machine transition an `accountingManager`/`controller` write drives — audited, but not approval-gated | transition-scoped hooks (`trigger: onTransition`, carrying prior state) or prior-state bindings in guards | medium | backlog |
| G-11 | ADR-010 harness | **The close-checklist suite leg:** §9 item 3's "close checklist completes only with all tasks resolved" needs the suite to observe the event-started process's tasks, but event-starts ride the spine asynchronously and the harness has no await/poll step — a `queryRecord Task` right after the CLOSING update races | The checklist's all-tasks-then-confirm shape is structural (the BPMN parallel join gates `confirmClose`) and pinned service-level by `BpmnProcessTests`; the authored suite covers the CLOSING write gate instead | an `awaitTasks`/poll step op (bounded retries over the inbox query) | medium | backlog |
| G-12 | P5 Reporting | **P&L amount slicing by account type:** report groupBy/filters bind direct fields of one entity — `JournalLine` cannot group by `account.type` (no lookup-path traversal in the report vocabulary), so revenue/expense nets over journal lines are not expressible as a report | `plSketch` is the structural section sketch over `Account` (type sections + count + drill-through to lines); the trial balance remains the amount surface, and the reconciliation suite pins the nets | lookup-path groupBy/filters in reports (join lowering), or a denormalize-on-write harvest stamping the target's classification | medium | backlog |
| G-13 | AR/AP | **AP bill document:** the `Invoice` entity is AR-shaped (required `customer` lookup, AR machine, AR aging) — a vendor bill document with its own numbering and subledger view is not authored | AP rides the GL — `JournalEntry` postings against AP control accounts with the `Vendor` subledger (the `ap-vendor-subledger` suite leg books one end to end) | a versioned `APBill` app template, or a polymorphic party lookup if a second dogfood flow demands one document shape | medium | backlog |
| G-14 | P3/P6 flows × connectors | **Idempotent scheduled pulls:** flow-side `createRecord` has no upsert/idempotency mode (the inbound-webhook mapping's `mode: upsert` + `keyFields` has no flow-IR twin) and scheduled templates have no cursor bindings, so the `bankFeedSync` flow's `since` is a constant and a re-pull of an already-settled transaction rejects on `Payment.number`'s unique index | The provider's feed window bounds v1; a duplicate rejects audibly (the scheduled run records the failure — never a silent double-apply), and the inbound `paymentsFeed` webhook (`mode: upsert`, `keyFields: [number]`) stays the idempotent push path the `bankFeed` suite pins | `createRecord { mode: upsert, keyFields }` mirroring the webhook mapping + cursor bindings in scheduled templates (e.g. `${fire.cursor}`) | medium | backlog |
| G-15 | P1 Data Modeling | **Roll-ups cannot filter by child state:** `qtyOnHand`/`inventoryValue` must sum only `POSTED` movements, but `SUM(movements.qty)` aggregates every child row from its create — the machine pins `DRAFT` at create, so a movement's qty counts before it is posted, and the costing flow (running at the POSTED update) read an Item that already included the issue's own qty | The `costMovement` flow discounted the row's own quantity (`preQty = qtyOnHand - $record.qty`, then the flow twin) before dividing — correct only while the roll-ups aggregated every row; found live running the suites (the authored script read qty 6 for a 10-qty item and priced the issue at 50/6) | conditional roll-ups — `SUM(movements.qty WHERE status = 'POSTED')`, or a `stampOnTransition`-scoped roll-up recompute | **high** | closed — shipped as the versioned platform feature (spec §3.5, 2026-08-27): the roll-up grammar grows an optional AND-joined WHERE over the DSL leaf vocabulary; the store and inline-create aggregation paths filter identically; save validation joins the rule matrix. **Adopted by the corpus 2026-09-04 (the 42nd pass):** `Item.qtyOnHand`/`inventoryValue` carry the WHERE clause and the flow's manual row discount is retired (the divisor is the plain bound view; the pinned costing numbers are unchanged) |

## Confirmed harvests (§3 — shipped as platform features this phase)

- **`freezeOnTerminal` (§3.1)** — confirmed by the GL module: `JournalEntry` and
  `StockLedger` freeze their whole documents in terminal states
  (`RECORD_FROZEN` 4013 on updates, deletes, and child writes naming the frozen
  parent); the `Invoice` deliberately does **not** freeze — §2's pin: freeze binds
  the journal entry, not the invoice, so settlement decrements `amountOutstanding`
  on the POSTED invoice (the `bankFeed` suite's leg). Verified by
  `FreezePeriodTests` + the `controls` suite.
- **`PeriodLock` (§3.2)** — confirmed by period close: dated writes resolve their
  period by date-range lookup; `CLOSED` periods reject with `PERIOD_LOCKED` 4014;
  **`CLOSING` blocks postings unless `closeJournal` is set** (§4's soft close — the
  `restrictedStatus`/`exemptField` growth, authored app metadata, no platform
  special-casing); reopening (§4's audited transition) deactivates the lock;
  nothing is ever un-frozen. Verified by `FreezePeriodTests` + the `controls` suite.

## Script ratio at exit review (§9 item 7)

4 hooks authored, **all declarative since 2026-09-03**: the 3 posting/scheduled flows
plus the weighted-average costing flow (the §3.7 `bind` primitive — the G-2 harvest —
took the last script's cross-record arithmetic declaratively; the `Payment` scheduled
hook joins the denominator per the thirty-first pass's honest recount). Logic surface:
5 validations, 1 formula, 4 roll-ups, 3 machines. **The ≤ 20% budget holds — 0 of 4 =
0% app-level, the Inventory module 0/1.** The exit state was 1/4 = 25% under G-2's
reviewed exception (the rule-3 primitive-candidate review); the harvest resolved it,
`scriptBudget` pins the compliant numbers so the ratio can never quietly grow back,
and change-set review reports the per-module ratios (`scriptRatio.modules`).
