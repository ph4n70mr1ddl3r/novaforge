{
  "name": "@novaforge/builder-ui",
  "version": "0.1.0",
  "private": true,
  "description": "The builder application (PHASE-2 §8): entity builder, page builder (canvas + property panel + live preview through the real renderer), RBAC/field-security editors, tenant onboarding — plus the Phase 8 lifecycle screens (change-set review, gated promotion, i18n editor) that ride the same design-time surface.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@novaforge/shared": "workspace:*",
    "@tanstack/react-query": "^5.90.0",
    "react": "~19.2.0",
    "react-dom": "~19.2.0"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.0",
    "@types/node": "^26.2.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.1.0",
    "axe-core": "^4.11.0",
    "jsdom": "^26.1.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.0",
    "vitest": "^3.2.4"
  }
}
