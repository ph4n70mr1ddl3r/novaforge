{
  "name": "@novaforge/runtime-ui",
  "version": "0.1.0",
  "private": true,
  "description": "The runtime application shell (PHASE-2 §6): metadata-driven pages through the shared renderer — nav by module, auto list/form/detail pages with saved-page overlays, the approval inbox (PHASE-4 §5), report dashboards (PHASE-5 §5), and the runtime i18n fallback chain (PHASE-8 §7).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=/runtime/",
    "test": "vitest run",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@novaforge/shared": "workspace:*",
    "@tanstack/react-query": "^5.90.0",
    "react": "~19.2.0",
    "react-dom": "~19.2.0"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.0",
    "@types/node": "^26.2.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.1.0",
    "axe-core": "^4.11.0",
    "jsdom": "^26.1.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.0",
    "vitest": "^3.2.4"
  }
}
