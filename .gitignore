# Maven (multi-module per ADR-005; script-only wrapper — PHASE-0 T2)
target/

# Java
*.class
*.jar
hs_err_pid*
replay_pid*
!.mvn/wrapper/maven-wrapper.jar

# Node / frontend (pnpm workspace + Vite, Playwright E2E — PHASE-2 §2)
node_modules/
dist/
.vite/
*.tsbuildinfo
test-results/
playwright-report/

# IDEs
.idea/
*.iml
.vscode/
.settings/
.classpath
.project
.factorypath
*.swp

# OS
.DS_Store
Thumbs.db

# Logs & temp
*.log
tmp/
temp/

# Local env & secrets
.env
.env.*
*.local
application-local.yml
application-local.yaml
secrets/
*.pem
*.key

# Local Kubernetes / deploy scratch (Kind-on-Podman — ARCHITECTURE §6)
kubeconfig*
.kind/

# Podman
.podman/

# Reports & coverage
coverage/
.nyc_output/
reports/

# Compose env overrides (secrets — never commit)
deploy/compose/.env
deploy/compose/.env.*
